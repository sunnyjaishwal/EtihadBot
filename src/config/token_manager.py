#{1:["x-d-token","access_token"]}

# create token manager class 
# initialize the variable
# create token_builder instance method
# get complete x-d token and access-token response
# prepare response for cache
# get the expiry time
# set the value to cache


class Token_Manager:
    
    def __init__(self):
        
    
    
    def token_builder(self):
        return 1
        
        
        
if __name__ == '__main__':
   object = Token_Manager() 